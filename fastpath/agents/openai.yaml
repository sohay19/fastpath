interface:
  display_name: "Fast Path For Plan Docs"
  short_description: "Minimize plan reads with task fast paths and indexes"
  default_prompt: "Use $fastpath to read or update only the required plan task while keeping indexes and status folders correct."
