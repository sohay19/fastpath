interface:
  display_name: "Fast Path For Plan Docs"
  short_description: "Minimize plan-reading cost with low-context fast paths"
  default_prompt: "Use $fastpath: for structured routine tasks run read-fastpath first with explicit -SnapshotPath/-PlanReadme and -Compact; no hand-built reads unless helper fails; final one line."
