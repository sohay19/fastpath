interface:
  display_name: "Fast Path For Plan Docs"
  short_description: "Minimize plan-reading cost with low-context fast paths"
  default_prompt: "Use $fastpath: read SNAPSHOTS once; trust valid line ranges; read plan state once only if required; update narrowly; final one line."
