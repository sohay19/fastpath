interface:
  display_name: "Fast Path For Plan Docs"
  short_description: "Minimize plan-reading cost with low-context fast paths"
  default_prompt: "Use $fastpath: one bundled first read when possible (SNAPSHOTS Fast Path + required state + exact detail slice); trust valid line ranges; no rediscovery/todo/interim summaries; update narrowly; final one line."
