interface:
  display_name: "Fast Path For Plan Docs"
  short_description: "Minimize plan-reading cost with low-context fast paths"
  default_prompt: "Use $fastpath: run .\\tools\\fastpath-read.ps1 -SnapshotPath .\\SNAPSHOTS.md -Compact once; no SKILL.md reread, no command parsing; final one line."
