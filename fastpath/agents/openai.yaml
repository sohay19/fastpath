interface:
  display_name: "Fast Path For Plan Docs"
  short_description: "Minimize plan-reading cost with task fast paths and indexes"
  default_prompt: "Use $fastpath to read/update only the required plan task, keep outputs compact, and preserve Raw/Uncached/Cost-equivalent benchmark rules."
